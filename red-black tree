定义：
1.每个节点不是红色就是黑色的；

 2.根节点总是黑色的；

3.如果节点是红色的，则它的子节点必须是黑色的（反之不一定）；

4.从根节点到叶节点或空子节点的每条路径，必须包含相同数目的黑色节点（即相同的黑色高度）


优势：即时在最坏的情况下也能保证也能保证插入删除的时间复杂度为O(logN)


应用：
关联数组中，
red-black tree并行使用 C++  中的 set map multiset multimap  红黑树: 平衡二叉树，广泛用在C++的STL中。如map和set都是用红黑树实现的。Java用它来实现TreeMap

B/B+树: 用在磁盘文件组织 数据索引和数据库索引，多路平衡查找树
Trie树(字典树): 用在统计和排序大量字符串，如自动机。字典匹配，模式匹配，正则表达式都需要用到。
AVE tree


